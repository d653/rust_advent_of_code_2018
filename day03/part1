cat input  | cut -d ' ' -f 3-4  | tr ',' ' '  | tr -d ':' | tr 'x' ' ' | awk '{ for(i=$1;i<$1+$3;i++)for(j=$2;j<$2+$4;j++)m[i][j]++; } END { for(i=0;i<1000;i++)for(j=0;j<1000;j++)if(m[i][j]>1)c++; print c }'

